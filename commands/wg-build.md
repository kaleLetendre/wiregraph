---
description: ↳ alias of /wiregraph-build
argument-hint: "[target-dir] (defaults to the current directory)"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-build**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-build.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
