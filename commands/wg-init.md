---
description: ↳ alias of /wiregraph-init
argument-hint: "[target-dir] [--no-directive] (target defaults to the current project root)"
allowed-tools: Bash, Read, Edit, AskUserQuestion
---
Short alias for **/wiregraph-init**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-init.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
