---
description: ↳ alias of /wiregraph-linearize
argument-hint: "<Class.method | method | ClassName> · [--breadth tight|medium|full] [--depth N] [--file <hint>] [--out <path>]"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-linearize**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-linearize.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
