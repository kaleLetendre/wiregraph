---
description: ↳ alias of /wiregraph-list
argument-hint: "(no arguments; the graph is resolved from the cwd)"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-list**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-list.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
