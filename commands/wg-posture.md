---
description: ↳ alias of /wiregraph-posture
argument-hint: "<off|conservative|balanced|aggressive> [target-dir]"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-posture**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-posture.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
