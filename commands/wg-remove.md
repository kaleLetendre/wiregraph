---
description: ↳ alias of /wiregraph-remove
argument-hint: "[target-dir] (defaults to the active project)"
allowed-tools: Bash, Read, AskUserQuestion
---
Short alias for **/wiregraph-remove**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-remove.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
