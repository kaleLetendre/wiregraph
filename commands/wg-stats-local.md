---
description: ↳ alias of /wiregraph-stats-local
argument-hint: "(no args — uses the active project; use /wiregraph-stats for the whole machine)"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-stats-local**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-stats-local.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
