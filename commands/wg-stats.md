---
description: ↳ alias of /wiregraph-stats
argument-hint: "(no args — uses the active project)"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-stats**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-stats.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
