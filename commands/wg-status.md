---
description: ↳ alias of /wiregraph-status
argument-hint: "(no args — checks the active project)"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-status**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-status.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
