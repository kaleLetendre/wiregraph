---
description: ↳ alias of /wiregraph-unlink
argument-hint: "<linked-dir> (the member to remove; this graph is resolved from the cwd)"
allowed-tools: Bash, Read, AskUserQuestion
---
Short alias for **/wiregraph-unlink**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-unlink.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
