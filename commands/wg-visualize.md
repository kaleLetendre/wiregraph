---
description: ↳ alias of /wiregraph-visualize
argument-hint: "[target-dir] · add --all for the full call graph, or --contract \"<name>\" for one seam"
allowed-tools: Bash, Read
---
Short alias for **/wiregraph-visualize**. Run that command now:

Read `${CLAUDE_PLUGIN_ROOT}/commands/wiregraph-visualize.md` and follow its body
(everything after the frontmatter) exactly as written. Wherever it refers to
`$1`, `$2`, or "arguments", use the arguments passed to THIS invocation:

$ARGUMENTS
